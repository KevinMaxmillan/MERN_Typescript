export enum ApiEndpoints {
    REGISTER = "/register",
    LOGIN = "/login",
    LOGOUT = "/logout",
    PROFILE = "/profile",
    REFRESH_TOKEN = "/refresh",
    POSTS = "https://jsonplaceholder.typicode.com/posts",
    
    
  }